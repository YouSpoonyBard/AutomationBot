pipeline {
    agent any
    stages {
        stage('Initial pipeline test') { 
            steps {
                sh "pwd"
                sh "whoami"
                echo "hello world"
            }
        }
        stage('Build') {
            steps {
                sh "node mybot.js"
            }
        }
    }
}